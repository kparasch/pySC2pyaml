# pySC2pyaml

Bindings between the pySC control system and PyAML.

## Running tests

Install the package and development dependencies in your Python environment:

```sh
pip install -e '.[dev]'
pytest
```
